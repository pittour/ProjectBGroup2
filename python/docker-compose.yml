---
version: '3'
services:
  microservice:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "8083:80"
      - "8084:443"
    networks:
      - projetB

networks:
  projetB:
    external: true